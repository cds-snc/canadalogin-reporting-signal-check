# Secrets and connection config
.env

# Quarto build artifacts
.quarto/
*_files/
/_book/
*.rmarkdown

# Rendered deliverables (source-only repo; the all-in-one HTML is shared, not committed)
reports/*.html
reports/out/
*.html
# ...except the head fragment that keeps the published reports out of search
# indexes. It is source, not output.
!reports/_noindex.html

# R / RStudio
.Rproj.user/
.Rhistory
.RData
.Ruserdata

# macOS
.DS_Store
Icon?
